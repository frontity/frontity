import * as useInfiniteScroll from "../use-infinite-scroll";

test("should fetch content if first reference is in view", () => {});

test("should change route if second reference is in view", () => {});

test("should do nothing if IntersectionObserver is not supported.", () => {});
