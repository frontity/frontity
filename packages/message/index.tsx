// Simple function that appends an invitation to join »Frontity community.
const msg = (message: string) => `${message}

If you have any questions, join our community at https://community.frontity.org/.
`;

export default msg;
