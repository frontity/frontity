/**
 * Helper to evaluate that some JavaScript variable has a specific type.
 *
 * @param ExpectedType Any type that you want to evaluate.
 */
export const expectType = <ExpectedType>(value: ExpectedType) => {
  value;
};
