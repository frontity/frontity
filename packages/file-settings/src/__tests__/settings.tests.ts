import getSettings from "../settings";

describe("File Settings › Settings", () => {
  test("`getSettings` returns settings", () => {});
  test("`getPackages` returns a list of packages", () => {});
  test("`getSettings` gets settings from the root directory", () => {});
  test("`getSettings` gets settings from the default files", () => {});
});
