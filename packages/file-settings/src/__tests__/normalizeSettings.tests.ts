import normalizeSettings, { normalizePackage } from "../normalizeSettings";

describe("`normalizePackage` should work when", () => {
  test("param `pkg` is a string", () => {});
  test("param `pkg` is an object", () => {});
});

describe("`normalizeSettings` should work when", () => {
  test("param `settings` is an array", () => {});
  test("param `settings` is an object", () => {});
});
