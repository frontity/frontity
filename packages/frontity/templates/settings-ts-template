import { Settings } from "frontity/types";
import WpSource from "@frontity/wp-source/types";
import Theme from "$theme$/types";

const settings: Settings<Theme | WpSource> = $settings$;

export default settings;
